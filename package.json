{
  "name": "bastet-static",
  "private": true,
  "version": "0.0.1",
  "description": "A static UI for Bastet proxy",
  "license": "MIT",
  "devDependencies": {
    "karma": "~0.10",
    "http-server": "^0.6.1",
    "bower": "^1.3.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower update",
    "prestart": "npm update",
    "test": "exit 0"
  }
}
